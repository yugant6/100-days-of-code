#A tuple is a collection which is ordered and unchangeable. In Python tuples are written with round brackets.

thistuple = ("apple", "banana", "cherry")
print(thistuple)

print(thistuple[1])

#thistuple[1] = "blackcurrant" # Unchangeble
#print(thistuple)

thistuple = tuple(("Yugant", "Satish", "Geeta"))
print(len(thistuple))